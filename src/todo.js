export default class Todo {
	constructor(title, dueDate, description, notes, priority) {
		this.title = title;
		this.dueDate = dueDate;
		this.description = description;
		this.notes = notes;
		this.priority = priority;
	}
}
